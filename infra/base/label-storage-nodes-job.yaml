apiVersion: batch/v1
kind: Job
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
  name: ldap-conf-bundle
  namespace: default
spec:
  template:
    spec:
      containers:
        - image:  openshift/origin-cli
          command:
            - /bin/bash
            - -c
            - |
              #!/usr/bin/env bash
              set -e
                oc adm policy add-cluster-role-to-group cluster-admin 'CN=ocp.ifdsnet.int_Admin,OU=Infrastructure Tools,OU=Groups Management,OU=Toronto,OU=IFDS,DC=canada01,DC=ifdsnet,DC=int'
          imagePullPolicy: Always
          name: ldap-conf-bundle
      dnsPolicy: ClusterFirst
      restartPolicy: Never
      serviceAccount: ldap-conf
      serviceAccountName: ldap-conf
      terminationGracePeriodSeconds: 30
